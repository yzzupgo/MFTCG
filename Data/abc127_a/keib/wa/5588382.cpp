#include <bits/stdc++.h>
using namespace std;
int main() {
std::cin.tie(nullptr);
std::ios::sync_with_stdio(false);
int64_t a, b;
if (a <- 5) {
std::cout << 0 << std::endl;
} else if (a <= 12) {
std::cout << b / 2 << std::endl;
} else {
std::cout << b << std::endl;
}
return 0;
}
